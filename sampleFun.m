# example of a function,
# first you have to load function with source('sampleFun.m')
# and then invoke it with testFun(args)

function testFun(arg)
    printf(" %s: you've invoked testFun\n",arg);
endfunction
